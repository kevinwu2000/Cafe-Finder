{
  "scripts": {
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn start",
    "install:prod": "cd frontend && yarn install --freeze-lockfile && cd ../backend && yarn install --freeze-lockfile",
    "build": "cd frontend && yarn build && cd ../backend && yarn start",
    "deploy": "cd frontend && yarn build"
  },
  "name": "WP_final_project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT"
}
